<?xml version="1.0" encoding="utf-8"?>
<odoo>

<!-- Blog Accordion Snippet Template -->
<template id="s_blog_accordion" name="Blog Accordion">
    <section class="s_blog_accordion pt48 pb48">
        <div class="container">
            <h2 class="text-center mb-5">Explore Our List of Blogs</h2>
            <div id="blogAccordion" class="accordion">
                <t t-set="blogs" t-value="request.env['blog.blog'].search([])"/>
                <t t-foreach="blogs" t-as="blog">
                    <div class="card border-0 mb-2 shadow-sm">
                        <div class="card-header bg-white p-0" t-attf-id="heading#{blog.id}">
                            <h5 class="mb-0">
                                <button class="btn btn-link btn-block text-left d-flex justify-content-between align-items-center p-3 text-dark font-weight-bold" 
                                        type="button" 
                                        data-toggle="collapse" 
                                        t-attf-data-target="#collapse#{blog.id}" 
                                        aria-expanded="false" 
                                        t-attf-aria-controls="collapse#{blog.id}">
                                    <span t-field="blog.name"/>
                                    <i class="fa fa-chevron-down small"/>
                                </button>
                            </h5>
                        </div>

                        <div t-attf-id="collapse#{blog.id}" 
                             class="collapse" 
                             t-attf-aria-labelledby="heading#{blog.id}" 
                             data-parent="#blogAccordion">
                            <div class="card-body p-0">
                                <ul class="list-group list-group-flush">
                                    <t t-set="posts" t-value="request.env['blog.post'].search([('blog_id', '=', blog.id), ('website_published', '=', True)])"/>
                                    <t t-if="not posts">
                                        <li class="list-group-item text-muted small">No posts available in this category.</li>
                                    </t>
                                    <t t-foreach="posts" t-as="post">
                                        <li class="list-group-item py-2 px-4 border-0">
                                            <a t-att-href="post.website_url" class="text-info text-decoration-none">
                                                <i class="fa fa-caret-right mr-2"/>
                                                <span t-field="post.name"/>
                                            </a>
                                            <small class="text-muted d-block ml-3" t-field="post.post_date"/>
                                        </li>
                                    </t>
                                </ul>
                            </div>
                        </div>
                    </div>
                </t>
            </div>
        </div>
    </section>
</template>

<!-- Register the snippet -->
<template id="s_blog_accordion_registration" inherit_id="website.snippets" name="Blog Accordion Registration">
    <xpath expr="//div[@id='snippet_content']//div[contains(@class, 'o_panel_body')]" position="inside">
        <t t-snippet="premium_blog_cards.s_blog_accordion" t-thumbnail="/website/static/src/img/snippets_thumbs/s_faq_collapse.svg"/>
    </xpath>
</template>

</odoo>
